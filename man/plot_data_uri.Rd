% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot-image.R
\name{plot_data_uri}
\alias{plot_data_uri}
\title{Capture a plot as a PNG data URI (for \code{on_image()})}
\usage{
plot_data_uri(expr, width = 640, height = 400, res = 96)
}
\arguments{
\item{expr}{A plotting expression or a printable plot object (evaluated once,
inside the device).}

\item{width, height}{Image size in pixels. Defaults 640x400.}

\item{res}{Nominal resolution in ppi (affects text/line scaling). Default 96.}
}
\value{
A single string \code{"data:image/png;base64,..."}.
}
\description{
Convenience helper for the chart/plot path of a chat handler: renders a
plotting expression to an off-screen PNG and returns it as a
\verb{data:image/png;base64,...} string, ready to hand to the \code{on_image()}
callback so the plot appears inline in the conversation. This is the
recommended way to show charts (ggplot2, lattice, or base graphics) inline —
R's own plotting is used, and only an image is sent to the browser (no
client-side charting library is bundled).
}
\details{
\code{expr} is evaluated lazily inside the PNG graphics device:
\itemize{
\item \strong{base graphics} (e.g. \code{plot(...)}, \code{hist(...)}) draw during evaluation —
pass the plotting call directly.
\item \strong{ggplot2 / lattice} objects only draw when printed — pass the object
(e.g. \code{ggplot(df, aes(x, y)) + geom_point()}); it is auto-printed.
}

The graphics device is always closed, even if \code{expr} errors.
}
\examples{
\dontrun{
assistantUIServer("chat", handler = function(message, on_chunk, on_done, on_image, ...) {
  on_chunk("Here's the distribution:\n")
  on_image(plot_data_uri(hist(rnorm(1000))))                       # base graphics
  # on_image(plot_data_uri(ggplot2::qplot(speed, dist, data = cars)))  # ggplot2
  on_done()
})
}

}
\seealso{
\code{\link[=assistantUIServer]{assistantUIServer()}} for the \code{on_image()} callback.
}
