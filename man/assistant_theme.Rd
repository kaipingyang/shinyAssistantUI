% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme.R
\name{assistant_theme}
\alias{assistant_theme}
\title{Construct a theme for the assistant UI}
\usage{
assistant_theme(
  background = NULL,
  foreground = NULL,
  primary = NULL,
  primary_foreground = NULL,
  secondary = NULL,
  secondary_foreground = NULL,
  accent = NULL,
  accent_foreground = NULL,
  muted = NULL,
  muted_foreground = NULL,
  destructive = NULL,
  destructive_foreground = NULL,
  card = NULL,
  card_foreground = NULL,
  popover = NULL,
  popover_foreground = NULL,
  border = NULL,
  input = NULL,
  ring = NULL,
  radius = NULL
)
}
\arguments{
\item{background, foreground}{Base surface color and default text color.}

\item{primary, primary_foreground}{Primary accent (send button, user bubble).}

\item{secondary, secondary_foreground}{Secondary surfaces.}

\item{accent, accent_foreground}{Hover/active highlight color.}

\item{muted, muted_foreground}{Muted surfaces and secondary text.}

\item{destructive, destructive_foreground}{Error/danger color.}

\item{card, card_foreground}{Card surfaces (e.g. tool result cards).}

\item{popover, popover_foreground}{Popover/menu surfaces.}

\item{border, input}{Border color and input border color.}

\item{ring}{Focus ring color.}

\item{radius}{Corner radius as a CSS length (e.g. \code{"0.5rem"}, \code{"8px"}).
Passed through verbatim (not a color).}
}
\value{
A named list of CSS-ready theme tokens, suitable for the \code{theme}
argument of \code{\link[=assistantUIServer]{assistantUIServer()}}.
}
\description{
Builds a named list of theme tokens for \code{\link[=assistantUIServer]{assistantUIServer()}}'s \code{theme}
argument. Colors may be given in any format R understands (hex like
\code{"#2563eb"}, named colors like \code{"steelblue"}, or \code{rgb()} strings) and are
converted to the HSL-component format assistant-ui expects
(\code{"H S\% L\%"}, e.g. \code{"217 91\% 60\%"}). Values that are already in
HSL-component form are passed through unchanged.
}
\details{
assistant-ui uses shadcn-style semantic color tokens. Each token has a
matching \verb{*_foreground} companion used for text drawn on top of it.
}
\examples{
assistant_theme(primary = "#2563eb", radius = "0.75rem")
assistant_theme(background = "#0b1020", foreground = "#e5e7eb")

}
