% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tool-view.R
\name{assistant_tool_view}
\alias{assistant_tool_view}
\title{Declare how a tool call's arguments are rendered in the chat card}
\usage{
assistant_tool_view(
  kind = c("code", "diff"),
  field = NULL,
  lang = NULL,
  old_field = NULL,
  new_field = NULL,
  file_field = NULL
)
}
\arguments{
\item{kind}{The view kind: \code{"code"} (syntax-highlighted code block) or
\code{"diff"} (old/new unified diff).}

\item{field}{For \code{kind = "code"}: the argument name holding the code string.
Defaults to \code{"code"} on the client when omitted.}

\item{lang}{For \code{kind = "code"}: the syntax-highlighting language (e.g. \code{"r"},
\code{"sql"}, \code{"bash"}, \code{"python"}). Falls back to a neutral language when omitted.}

\item{old_field, new_field, file_field}{For \code{kind = "diff"}: the argument names
holding the old text, new text, and file name. Default to \code{"old_string"},
\code{"new_string"}, and \code{"file_path"} on the client when omitted.}
}
\value{
A named list \verb{list(argsView = <spec>)} suitable for a tool call's
\code{annotations}.
}
\description{
Builds the \code{argsView} tool-call annotation that tells the widget how to render
a tool call's \strong{arguments} (the region an approver reads), instead of the raw
JSON fallback. This is the server-side (R) extension point for tools whose
semantics only the author knows — primarily \code{\link[ellmer:tool]{ellmer::tool()}} tools (whose
\code{annotations} are forwarded automatically by \code{\link[=make_ellmer_handler]{make_ellmer_handler()}}) and any
custom \code{handler} that emits \code{on_tool_call(annotations = ...)}.
}
\details{
Claude Code's fixed built-in tools (\code{Bash}, \code{Edit}, \code{Write}, \code{run_r}, ...) are
rendered by built-in rules on the client and do \strong{not} need this.

The return value is a plain list; merge it into a tool call's annotations, e.g.
\code{on_tool_call(id, name, args, annotations = assistant_tool_view("code", field = "code", lang = "r"))}, or \code{c(other_annotations, assistant_tool_view(...))}.
}
\examples{
# Render a tool's `code` argument as an R code block:
assistant_tool_view("code", field = "code", lang = "r")

# A SQL tool whose query lives in the `sql` argument:
assistant_tool_view("code", field = "sql", lang = "sql")

# A custom edit-style tool rendered as a diff:
assistant_tool_view("diff", old_field = "before", new_field = "after")

\dontrun{
# With ellmer: declare the view in the tool's annotations (forwarded to the card).
ellmer::tool(
  run_sql,
  description = "Run a SQL query",
  arguments   = list(sql = ellmer::type_string("SQL to run")),
  annotations = assistant_tool_view("code", field = "sql", lang = "sql")
)
}
}
