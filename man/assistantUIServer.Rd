% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/server.R
\name{assistantUIServer}
\alias{assistantUIServer}
\title{Assistant UI Server Module}
\usage{
assistantUIServer(
  id,
  handler,
  show_thread_list = FALSE,
  persistence = c("client", "server", "none"),
  suggestions = list(),
  commands = list(),
  tools = list(),
  action_items = list(),
  ide_context_provider = NULL,
  workspace_search_provider = NULL,
  on_action = NULL,
  on_session_load = NULL,
  on_feedback = NULL,
  on_rename = NULL,
  on_open_file = NULL,
  on_run_in_console = NULL,
  on_edits = NULL,
  on_archive_session = NULL,
  on_delete_session = NULL,
  workspace_mode = FALSE,
  working_dir = NULL,
  native_picker = FALSE,
  on_pick_working_dir = NULL,
  on_set_working_dir = NULL,
  projects = NULL,
  on_save_project = NULL,
  on_remove_project = NULL,
  files_pane_follow = NULL,
  on_toggle_files_pane_follow = NULL,
  auto_run = NULL,
  on_toggle_auto_run = NULL,
  default_permission_mode = NULL,
  on_set_default_permission_mode = NULL,
  mode_visibility = NULL,
  on_set_mode_visibility = NULL,
  composer_density = NULL,
  on_set_composer_density = NULL,
  run_r_enabled = NULL,
  on_toggle_run_r = NULL,
  thread_max_width = NULL,
  show_usage = FALSE,
  context_window = NULL,
  usage_style = c("ring", "bar", "text"),
  latex = FALSE,
  code_theme = "one-light",
  strings = NULL,
  warming_label = NULL,
  welcome_message = NULL,
  assistant_avatar = list(fallback = "AI"),
  theme = NULL,
  dark_mode = FALSE,
  show_timestamps = FALSE,
  modal = FALSE,
  prewarm = FALSE,
  allow_warmup = TRUE,
  max_concurrent_runs = 1L
)
}
\arguments{
\item{id}{The module ID matching the \code{outputId} passed to \code{\link[=assistantUIOutput]{assistantUIOutput()}}.}

\item{handler}{A function with signature
\verb{function(message, thread_id, on_chunk, on_done, on_error, on_tool_call, on_tool_result, is_reload)} where:
\itemize{
\item \code{message} — character string of the user's message.
\item \code{thread_id} — character string identifying the current thread (for
multi-turn conversation routing).
\item \code{on_chunk(text)} — call repeatedly to stream response tokens.
\item \code{on_done()} — call once when the response is complete.
\item \code{on_error(msg)} — call to surface an error in the UI.
\item \code{on_tool_call(tool_call_id, tool_name, args, annotations)} — show a
tool call card. \code{args} should be a named list. \code{annotations} is an
optional named list controlling card appearance and result rendering.
Recognized keys:
\itemize{
\item \code{icon} — lucide icon name (\code{"search"}, \code{"database"}, \code{"code"}, …)
\item \code{title} — display name shown in the card header
\item \code{requiresApproval} — \code{TRUE} to show Approve/Deny buttons
\item \code{resultType} — how to render the result from \code{on_tool_result()}:
\code{"auto"} (default, JSON/text in \verb{<pre>}), \code{"markdown"},
\code{"table"}, \code{"code"}, \code{"image"}, \code{"file"}, \code{"html"}
\item \code{resultLang} — language for \code{resultType = "code"} (e.g. \code{"r"},
\code{"python"}, \code{"sql"}; default \code{"text"})
\item \code{resultFilename} — filename for \code{resultType = "file"} download
button (e.g. \code{"results.csv"}; default \code{"download"})
}
\item \code{on_tool_result(tool_call_id, result, is_error = FALSE)} — update the
tool card with the result.
\itemize{
\item \code{resultType = "table"}: pass \code{jsonlite::toJSON(df, auto_unbox = FALSE)}
\item \code{resultType = "file"}: pass base64 data URL
(\code{paste0("data:text/csv;base64,", jsonlite::base64_enc(chartr(...)))})
\item \code{resultType = "html"}: pass HTML string (rendered via
\code{dangerouslySetInnerHTML}; only use with trusted tool output)
}
\item \code{attachments} — list of named lists, one per file the user attached.
Each element has \code{type} (\code{"image"}, \code{"text"}, or \code{"file"}), \code{name}
(filename), \code{data} (data-URL string for images, plain text for text
files, base64 for other files), and optionally \code{contentType} (MIME
type). Empty list \code{list()} when no attachments are present.
\item \code{is_reload} — \code{TRUE} when the user clicked "regenerate"; the handler
receives the same \code{message} text and can remove the previous assistant
turn from the LLM history before re-running.
\item \code{is_cancelled} — zero-argument function that returns \code{TRUE} once the
user has clicked the stop button. Poll this inside your streaming loop
to implement true server-side cancellation:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{for (chunk in stream) \{
  if (is_cancelled()) break
  on_chunk(chunk)
\}
on_done()
}\if{html}{\out{</div>}}

For \href{https://github.com/kaipingyang/ClaudeAgentSDK}{ClaudeAgentSDK},
call \code{client$interrupt()} when \code{is_cancelled()} returns \code{TRUE}.
\item \code{wait_for_approval} — \verb{function(tool_call_id)} that returns a
\code{promises::promise} which resolves to \code{TRUE} (approved) or \code{FALSE}
(denied) once the user clicks Approve/Deny in the tool card UI.
Use inside a \code{coro::async} \code{on_tool_request} callback to pause
the stream until human approval:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{chat$on_tool_request(coro::async(function(request) \{
  on_tool_call(request@id, request@name, request@arguments,
              list(requiresApproval = TRUE))
  approved <- coro::await(wait_for_approval(request@id))
  if (!approved) ellmer::tool_reject("User denied the tool call.")
\}))
}\if{html}{\out{</div>}}
\item \code{register_cancel} — \verb{function(fn)} that stores a cancel callback for
the current thread. Call once before starting the stream with a function
that performs true HTTP cancellation (e.g.
\code{register_cancel(function() ctrl$cancel("Interrupted"))}).  When the
user clicks Stop, \code{assistantUIServer} calls \code{fn()} immediately so the
in-flight HTTP request is closed rather than silently drained.
}

All parameters except \code{message}, \code{on_chunk}, \code{on_done}, and \code{on_error}
are optional: handlers that omit them continue to work unchanged.

The handler may return a promise (from \code{promises} or \code{coro}) for async
streaming; errors from the promise are automatically forwarded via
\code{on_error}.}

\item{show_thread_list}{Logical. If \code{TRUE}, a thread list sidebar is shown
inside the widget for switching between conversations. Default \code{FALSE}
(backward-compatible).}

\item{persistence}{Where thread history is persisted. \code{"client"} (default)
restores and synchronizes browser \code{localStorage}; \code{"server"} treats
\code{send_sessions()} snapshots as authoritative and never accesses
\code{localStorage}; \code{"none"} keeps state only for the current page lifetime and
likewise never accesses \code{localStorage}.}

\item{suggestions}{List of starter suggestion bubbles shown before the first
message. Each element is a list with \code{prompt} (required, the text sent on
click) and optional \code{text} (display label, defaults to \code{prompt}).}

\item{commands}{List of slash-command definitions. Each element is a list
with \code{name} (e.g. \code{"summarize"}), \code{description}, \code{prompt} (the message
sent when the command is submitted), and optional \code{category} (group label
shown as a section header, such as \code{"Personal Skills"} or \code{"Project Skills"}).}

\item{tools}{List of tool definitions for the \@ mention menu. Each element
is a list with \code{name} and \code{description}. Typically mirrors the tools
registered with ellmer.}

\item{action_items}{List of action-type slash-command items. Unlike
\code{commands}, these do not send a message to the AI — instead, clicking them
fires \code{on_action(id)} on the R side, allowing arbitrary server logic.
Each element is a list with \code{section} (group label), \code{id} (unique string
passed to \code{on_action}), optional \code{command} (the slash name shown and matched
for exact direct input; defaults to \code{id}), \code{label}, and optional
\code{description}. Example:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{action_items = list(
  list(section = "Model",   id = "thinking-on",  label = "Enable thinking"),
  list(section = "Support", id = "view-docs",    label = "View help docs",
       description = "Open documentation")
)
}\if{html}{\out{</div>}}}

\item{ide_context_provider}{Optional zero-argument function sampled for every
new user submission. It may return active file/selection metadata; selection
text remains on the R side and is never sent to the browser.}

\item{workspace_search_provider}{Optional function accepting \code{query}, \code{kinds},
and \code{limit}, returning literal file/folder mention entries. Supplying it
advertises the workspace mention capability.}

\item{on_action}{\verb{function(id)} called when the user clicks an item from
\code{action_items}. \code{id} is the string from the item definition. Use this to
trigger server-side logic (e.g. toggle a setting, open a URL, call
\code{clear()}). \code{NULL} (default) means no handler is registered.}

\item{on_session_load}{Optional \verb{function(session_id, thread_id, send_thread)}
called when the frontend requests messages for a historical session thread.}

\item{on_feedback}{Optional \verb{function(message_id, type)} called when the user
clicks a positive or negative feedback button (\code{type} is \code{"positive"} or
\code{"negative"}).}

\item{on_rename}{Optional \verb{function(thread_id, title)} called when the user
renames a thread in the sidebar. The new title is already persisted
client-side (localStorage); use this to sync server-side session stores.}

\item{on_open_file}{Optional \verb{function(path, line = NULL)} called when the
user clicks a file reference in a tool card, or after the assistant edits a
file (the most recent successful edit of a run is revealed). The Claude
addin wires this to \code{rstudioapi::navigateToFile()}; leave \code{NULL} (default)
in browser contexts where no editor is available.}

\item{on_run_in_console}{Optional \verb{function(code)} called when the user clicks
"Run in Console" on an R code block. The Claude addin wires this to
\code{rstudioapi::sendToConsole(code, execute = TRUE)} so the code runs in the user's
live R session (visible, with their objects/packages/plots). When supplied, R code
blocks show a run button. Leave \code{NULL} (default) outside RStudio.}

\item{on_edits}{Optional \verb{function(edits)} called when the assistant proposes
file edits (used by the addin to show edit markers).}

\item{on_archive_session}{Optional \verb{function(session_id, archived)} called when
the user archives (\code{archived = TRUE}) or unarchives (\code{FALSE}) a session in the
sidebar. Use it to persist a server-authoritative soft-hide list so archived
sessions stay hidden across reopens (the Claude addin stores this per project).}

\item{on_delete_session}{Optional \verb{function(session_id)} called when the user
confirms deleting a session. This is destructive: the Claude addin wires it to
\code{ClaudeAgentSDK::delete_session()}, permanently removing the transcript from
disk. The UI requires an explicit confirmation before invoking it.}

\item{workspace_mode}{Logical. When \code{TRUE}, enables project-aware thread
metadata and navigation. Incoming requests may supply an optional \code{project}
snapshot; handlers, history callbacks, and thread-aware providers receive it
only when their formals declare \code{project} (or \code{...}), preserving existing
callback signatures. Defaults to \code{FALSE}.}

\item{working_dir}{Optional initial working directory shown in the
working-directory picker (addin).}

\item{native_picker}{Logical; whether a native directory chooser is available
(RStudio addin).}

\item{on_pick_working_dir}{Optional \verb{function()} invoked to open the native
working-directory picker.}

\item{on_set_working_dir}{Optional \verb{function(path)} called when the working
directory changes.}

\item{projects}{Optional character vector of saved working-directory favorites.}

\item{on_save_project}{Optional \verb{function()} to save the current directory as a
favorite.}

\item{on_remove_project}{Optional \verb{function(path)} to remove a saved favorite.}

\item{files_pane_follow}{Optional logical initial state of the "Files pane
follows working dir" toggle (\code{NULL} hides it).}

\item{on_toggle_files_pane_follow}{Optional \verb{function(value)} called when that
toggle changes.}

\item{auto_run}{Optional logical initial state of the auto-approve \code{run_r}
toggle (\code{NULL} hides it).}

\item{on_toggle_auto_run}{Optional \verb{function(value)} called when the auto-run
toggle changes.}

\item{default_permission_mode}{Optional character; default permission mode for
new conversations (addin preference, persisted).}

\item{on_set_default_permission_mode}{Optional \verb{function(mode)} called when the
default-mode preference changes.}

\item{mode_visibility}{Optional named list \code{list(showBypass=, showYolo=)}
controlling which risky modes appear in the mode selector.}

\item{on_set_mode_visibility}{Optional \verb{function(value)} called when mode
visibility changes.}

\item{composer_density}{Optional character \code{"comfortable"} (default) or
\code{"compact"} composer height preset.}

\item{on_set_composer_density}{Optional \verb{function(value)} called when the
composer height preset changes.}

\item{run_r_enabled}{Optional logical initial state of the \code{run_r} MCP tool
toggle (\code{NULL} hides it).}

\item{on_toggle_run_r}{Optional \verb{function(value)} called when the \code{run_r}
toggle changes.}

\item{thread_max_width}{Optional CSS length capping the chat content width
(e.g. \code{"44rem"}, \code{"800px"}). Default \code{NULL} = \strong{full width} (fills the
pane, like the Claude Code CLI / VS Code). Pass a length to center the
conversation in a fixed-width column (assistant-ui's classic readable
layout). Applies to messages, composer and the pinned-question bar together.}

\item{show_usage}{Logical (default \code{FALSE}); show the token-usage indicator.}

\item{context_window}{Optional integer context-window size for the usage
indicator.}

\item{usage_style}{Character; usage indicator style, one of \code{"ring"}, \code{"bar"},
\code{"text"}.}

\item{latex}{Logical (default \code{FALSE}); enable KaTeX math rendering.}

\item{code_theme}{Character string selecting the syntax-highlighting theme
for code blocks. Available light themes: \code{"one-light"} (default),
\code{"ghcolors"}, \code{"vs"}, \code{"solarized-light"}. Available dark themes:
\code{"vsc-dark-plus"}, \code{"dracula"}, \code{"nord"}, \code{"night-owl"}, \code{"one-dark"}.}

\item{strings}{Optional named list for overriding UI text (tooltips, labels,
placeholders). \code{NULL} (default) keeps all built-in English strings. Example
for a customized UI:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{strings = list(
  assistantMessage = list(
    copy   = list(tooltip = "Copy text"),
    reload = list(tooltip = "Regenerate answer")
  ),
  editComposer = list(
    send   = list(label = "Send now"),
    cancel = list(label = "Cancel")
  )
)
}\if{html}{\out{</div>}}}

\item{warming_label}{Optional character; cold-start indicator text (English).
Defaults to a generic "Starting…". e.g. "Starting codeagent…".}

\item{welcome_message}{Optional character; empty-state greeting. Defaults to
"How can I help you today?".}

\item{assistant_avatar}{Named list controlling the assistant's avatar.
Fields: \code{fallback} (1–2 character string or emoji shown when no image is
set), \code{src} (URL to an image), \code{alt} (alt text). Defaults to
\code{list(fallback = "AI")}. Example with custom image:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{assistant_avatar = list(src = "https://example.com/logo.png", fallback = "AI")
}\if{html}{\out{</div>}}}

\item{theme}{Optional named list of theme tokens to recolor the widget, e.g.#'   from \code{\link[=assistant_theme]{assistant_theme()}}. Colors may be hex/named/\code{rgb()} strings and are
converted to assistant-ui's HSL-component format. Applied as scoped CSS
variables on this widget only (multiple widgets can have different themes).
Example: \code{theme = assistant_theme(primary = "#2563eb")}.}

\item{dark_mode}{Dark color scheme control: \code{FALSE} (light, default), \code{TRUE}
(dark), or \code{"auto"} (follow the OS/browser \code{prefers-color-scheme}). A custom
\code{theme} overrides individual tokens in either mode.}

\item{show_timestamps}{Logical. If \code{TRUE}, each user/assistant message shows
its send time (HH:MM), derived from the message id. Defaults to \code{FALSE}.
\itemize{
\item \code{on_session_load} — \verb{function(session_id, thread_id, send_thread)} called
when the frontend requests historical messages for a session thread. Used
with \code{send_sessions()} (in the return value) to populate the sidebar with
Claude Code sessions and lazy-load their messages on click.
\itemize{
\item \code{session_id} — the Claude session UUID
\item \code{thread_id} — the UI thread ID (equals \code{session_id} for imported threads)
\item \code{send_thread(messages)} — call with a list of \code{ThreadMessageLike} objects
(each a named list with \code{id}, \code{role}, \code{content} fields) to populate the
thread.
}
}}

\item{modal}{Logical. If \code{TRUE}, renders the chat as a floating modal bubble
instead of an inline panel.}

\item{prewarm}{Logical (default \code{FALSE}). If \code{TRUE} and the \code{handler} exposes a
\code{warmup} attribute (e.g. \code{\link[=make_claude_handler]{make_claude_handler()}}), pre-connect the initial
thread's client on mount so the \strong{first} message on that thread isn't slowed
by the cold start (ClaudeSDKClient spawning the \code{claude} CLI subprocess).
Historical sessions are warmed independently after \code{send_thread()} succeeds,
regardless of this option; their warmups are deduplicated per thread and a
failed warmup may be retried on a later load. A brief cold-start indicator is
shown while connecting. Newly created blank threads remain lazy unless they
are the initial thread covered by this one-shot option.
\strong{Left \code{FALSE} by default on purpose}: enabling it makes app startup attempt
a backend connection at mount, so a slow/unreachable backend would stall the
open (the default lazy connect only happens on the first message). Turn on
only when the backend is reliably available at load.}

\item{allow_warmup}{Logical (default \code{TRUE}); allow per-thread cold-start
warmup of the handler. Warmups are deduplicated, limited to one at a time,
deferred while any foreground run is active or queued, and cancelled when
the Shiny session ends. The backend connect itself may still be synchronous.}

\item{max_concurrent_runs}{Positive integer global limit for runs in different
threads (default \code{1}, clamped to \code{8}). The requested limit is honored only
when \code{handler} explicitly declares
\code{attr(handler, "supports_concurrent_threads") <- TRUE}; all other handlers
remain globally serial for backward compatibility. Invocations within one
thread are always strict FIFO and never overlap.}
}
\value{
A list with a \code{clear()} function that creates a new thread in the UI,
\code{send_tool_call()} / \code{send_tool_result()} for manual tool card control, and
\code{send_sessions(sessions)} for injecting a list of historical session stubs
into the sidebar (each element: named list with \code{id}, \code{title}, \code{preview},
\code{createdAt} fields).
}
\description{
Handles the server-side logic for an \code{\link[=assistantUIOutput]{assistantUIOutput()}} widget: receives
user messages, calls a backend handler, and streams responses back to the UI.
}
