% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/server.R
\name{assistantUIServer}
\alias{assistantUIServer}
\title{Assistant UI Server Module}
\usage{
assistantUIServer(
  id,
  handler,
  show_thread_list = FALSE,
  suggestions = list(),
  commands = list(),
  tools = list(),
  code_theme = "one-light",
  strings = NULL,
  assistant_avatar = list(fallback = "AI")
)
}
\arguments{
\item{id}{The module ID matching the \code{outputId} passed to \code{\link[=assistantUIOutput]{assistantUIOutput()}}.}

\item{handler}{A function with signature
\verb{function(message, thread_id, on_chunk, on_done, on_error, on_tool_call, on_tool_result, is_reload)} where:
\itemize{
\item \code{message} — character string of the user's message.
\item \code{thread_id} — character string identifying the current thread (for
multi-turn conversation routing).
\item \code{on_chunk(text)} — call repeatedly to stream response tokens.
\item \code{on_done()} — call once when the response is complete.
\item \code{on_error(msg)} — call to surface an error in the UI.
\item \code{on_tool_call(tool_call_id, tool_name, args)} — show a tool call card
(args should be a named list).
\item \code{on_tool_result(tool_call_id, result, is_error = FALSE)} — update the
tool card with the result.
\item \code{attachments} — list of named lists, one per file the user attached.
Each element has \code{type} (\code{"image"}, \code{"text"}, or \code{"file"}), \code{name}
(filename), \code{data} (data-URL string for images, plain text for text
files, base64 for other files), and optionally \code{contentType} (MIME
type). Empty list \code{list()} when no attachments are present.
\item \code{is_reload} — \code{TRUE} when the user clicked "regenerate"; the handler
receives the same \code{message} text and can remove the previous assistant
turn from the LLM history before re-running.
\item \code{is_cancelled} — zero-argument function that returns \code{TRUE} once the
user has clicked the stop button. Poll this inside your streaming loop
to implement true server-side cancellation:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{for (chunk in stream) \{
  if (is_cancelled()) break
  on_chunk(chunk)
\}
on_done()
}\if{html}{\out{</div>}}

For \link[=https://github.com/kaipingyang/ClaudeAgentSDK]{ClaudeAgentSDK},
call \code{client$interrupt()} when \code{is_cancelled()} returns \code{TRUE}.
\item \code{wait_for_approval} — \verb{function(tool_call_id)} that returns a
\code{promises::promise} which resolves to \code{TRUE} (approved) or \code{FALSE}
(denied) once the user clicks Approve/Deny in the tool card UI.
Use inside a \code{coro::async} \code{on_tool_request} callback to pause
the stream until human approval:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{chat$on_tool_request(coro::async(function(request) \{
  on_tool_call(request@id, request@name, request@arguments,
              list(requiresApproval = TRUE))
  approved <- coro::await(wait_for_approval(request@id))
  if (!approved) ellmer::tool_reject("User denied the tool call.")
\}))
}\if{html}{\out{</div>}}
}

All parameters except \code{message}, \code{on_chunk}, \code{on_done}, and \code{on_error}
are optional: handlers that omit them continue to work unchanged.

The handler may return a promise (from \code{promises} or \code{coro}) for async
streaming; errors from the promise are automatically forwarded via
\code{on_error}.}

\item{show_thread_list}{Logical. If \code{TRUE}, a thread list sidebar is shown
inside the widget for switching between conversations. Default \code{FALSE}
(backward-compatible).}

\item{suggestions}{List of starter suggestion bubbles shown before the first
message. Each element is a list with \code{prompt} (required, the text sent on
click) and optional \code{text} (display label, defaults to \code{prompt}).}

\item{commands}{List of slash-command definitions. Each element is a list
with \code{name} (e.g. \code{"summarize"}), \code{description}, and \code{prompt} (the message
sent immediately when the command is selected).}

\item{tools}{List of tool definitions for the \@ mention menu. Each element
is a list with \code{name} and \code{description}. Typically mirrors the tools
registered with ellmer.}

\item{code_theme}{Character string selecting the syntax-highlighting theme
for code blocks. Available light themes: \code{"one-light"} (default),
\code{"ghcolors"}, \code{"vs"}, \code{"solarized-light"}. Available dark themes:
\code{"vsc-dark-plus"}, \code{"dracula"}, \code{"nord"}, \code{"night-owl"}, \code{"one-dark"}.}

\item{strings}{Optional named list for overriding UI text (tooltips, labels,
placeholders). \code{NULL} (default) keeps all built-in English strings. Example
for a Chinese UI:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{strings = list(
  assistantMessage = list(
    copy   = list(tooltip = "复制"),
    reload = list(tooltip = "重新生成")
  ),
  editComposer = list(
    send   = list(label = "发送"),
    cancel = list(label = "取消")
  )
)
}\if{html}{\out{</div>}}}

\item{assistant_avatar}{Named list controlling the assistant's avatar.
Fields: \code{fallback} (1–2 character string or emoji shown when no image is
set), \code{src} (URL to an image), \code{alt} (alt text). Defaults to
\code{list(fallback = "AI")}. Example with custom image:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{assistant_avatar = list(src = "https://example.com/logo.png", fallback = "AI")
}\if{html}{\out{</div>}}}
}
\value{
A list with a \code{clear()} function that creates a new thread in the UI.
}
\description{
Handles the server-side logic for an \code{\link[=assistantUIOutput]{assistantUIOutput()}} widget: receives
user messages, calls a backend handler, and streams responses back to the UI.
}
