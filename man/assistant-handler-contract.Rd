% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/handler-contract.R
\name{assistant-handler-contract}
\alias{assistant-handler-contract}
\title{The assistantUIServer handler contract}
\description{
The \code{handler} passed to \code{\link[=assistantUIServer]{assistantUIServer()}} is the sole integration point
between a chat backend and the UI. This page freezes that contract so the
backend layer (\code{make_ellmer_handler()}, \code{make_claude_handler()}, ...) can be
developed — and eventually extracted into a standalone, UI-agnostic package —
independently of the Shiny widget.
}
\section{Signature}{

The handler is a function (optionally \code{coro::async}) that declares any subset
of the following parameters, plus \code{...}. \code{assistantUIServer()} injects only
the parameters the handler actually declares (matched by name via
\code{names(formals(handler))}), so unused callbacks may be omitted safely.

\if{html}{\out{<div class="sourceCode r">}}\preformatted{handler <- function(message, thread_id,
                    on_chunk, on_done, on_error,
                    on_tool_call, on_tool_call_start, on_tool_call_delta,
                    on_tool_result, on_thinking,
                    on_source, on_image, on_artifact,
                    attachments, is_reload, is_cancelled,
                    wait_for_approval, register_cancel, ...) \{ ... \}
}\if{html}{\out{</div>}}
}

\section{Inputs}{

\itemize{
\item \code{message} — character. The user's text (slash commands already expanded).
\item \code{thread_id} — character. The UI thread this run belongs to.
\item \code{attachments} — list of uploaded attachments (image/text/file).
\item \code{is_reload} — logical. \code{TRUE} when regenerating a previous reply.
\item \code{is_cancelled} — \verb{function()} returning \code{TRUE} if the user hit Stop.
\item \code{wait_for_approval} — \verb{function(tool_call_id)} returning a promise that
resolves with the user's approval decision (for \code{requiresApproval} tools).
\item \code{register_cancel} — \verb{function(fn)} to register a cancel/interrupt callback
invoked when the user hits Stop.
}
}

\section{Output callbacks}{

\itemize{
\item \code{on_chunk(text)} — stream a response token.
\item \code{on_done(suggestions = list())} — finish; optional followup suggestions.
\item \code{on_error(message)} — surface an error.
\item \code{on_thinking(text)} — stream reasoning/thinking content.
\item \code{on_tool_call(tool_call_id, tool_name, args, annotations)} — a completed
tool call (whole-package path).
\item \code{on_tool_call_start(tool_call_id, tool_name, annotations)} /
\code{on_tool_call_delta(tool_call_id, delta)} — streaming tool-args path.
\item \code{on_tool_result(tool_call_id, result, is_error)} — a tool's result.
\item \code{on_source(url, title, id)} — attach a citation/source footnote.
\item \code{on_image(image)} — render an inline image (URL or data URI).
\item \code{on_artifact(id, title, content, type, lang)} — open/update a side-panel
artifact (\code{type}: markdown/code/html/text).
}
}

\section{Runtime expectation (for non-Shiny consumers)}{

An \code{coro::async} handler suspends on \code{await}/promises and therefore requires
an event loop that services \code{later} callbacks to make progress. Shiny's
\code{ExtendedTask} (used by \code{\link[=assistantUIServer]{assistantUIServer()}}) provides this automatically. A
non-Shiny consumer of the backend layer must pump the loop itself (e.g.
\code{later::run_now()} in a loop) for streaming and \code{wait_for_approval} to work.
}

\seealso{
\code{\link[=assistantUIServer]{assistantUIServer()}}, \code{\link[=make_ellmer_handler]{make_ellmer_handler()}}, \code{\link[=make_claude_handler]{make_claude_handler()}}
}
\keyword{internal}
