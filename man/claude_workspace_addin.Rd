% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/addin.R
\name{claude_workspace_addin}
\alias{claude_workspace_addin}
\title{Open a multi-project Claude Workspace inside RStudio}
\usage{
claude_workspace_addin(
  project = NULL,
  projects = NULL,
  viewer = c("pane", "dialog", "browser"),
  permission_mode = c("default", "plan", "acceptEdits", "bypassPermissions"),
  prewarm = FALSE,
  models = NULL,
  options = NULL,
  background = TRUE
)
}
\arguments{
\item{project}{Project root (Claude's working directory). Defaults to the active RStudio
project, else \code{\link[=getwd]{getwd()}}.}

\item{projects}{Optional character vector of additional project directories.
Paths are canonicalized and deduplicated; missing paths remain registered
but are not queried until they become available.}

\item{viewer}{Where to show the gadget: \code{"pane"} (Viewer pane, default), \code{"dialog"}, or
\code{"browser"}. Forced to \code{"browser"} when not running in RStudio.}

\item{permission_mode}{Claude tool-use permission policy:
\describe{
\item{\code{"default"}}{File edits and shell commands require per-action approval via the
in-chat approval card (safest — recommended for shared/production projects).}
\item{\code{"plan"}}{Read-only analysis and planning; edits are not permitted.}
\item{\code{"acceptEdits"}}{File edits are auto-approved; shell commands still prompt.}
\item{\code{"bypassPermissions"}}{All tool calls run without prompts (fastest, use with care).}
}
Ignored when \code{options} is supplied directly.}

\item{prewarm}{Logical (default \code{FALSE}). If \code{TRUE}, pre-connect the Claude CLI at launch so
the first message isn't slowed by the cold start (see \code{\link[=assistantUIServer]{assistantUIServer()}}).
Note: leave \code{FALSE} when the in-process \code{run_r} MCP tool is active — warming the
connection ahead of the first message triggers a CLI-side stall (see Plan 22 notes).}

\item{models}{Optional character vector of model names/aliases to offer in the Settings
"Model" selector (e.g. \code{c("sonnet", "opus")}). A "Default" option is always prepended.
Omit to use the built-in tiers (Default/Haiku/Sonnet/Opus). Switching is a live
\code{set_model} (no reconnect).}

\item{options}{Optional \link[ClaudeAgentSDK:ClaudeAgentOptions]{ClaudeAgentSDK::ClaudeAgentOptions} to fully override all defaults.}

\item{background}{Logical (default \code{TRUE}). Run the chat as an RStudio/Positron
\strong{background job} (via \code{\link[rstudioapi:jobRunScript]{rstudioapi::jobRunScript()}}) shown in the Viewer, so the R console
stays free while you chat. IDE integration (editor context, open-file, Files pane, markers,
save-before-edit) still works from the job via child-process \code{rstudioapi}. Falls back to the
classic blocking gadget when background jobs aren't available (e.g. plain R / browser). Set
\code{FALSE} to force the classic gadget.}
}
\value{
Invisibly, the result of \code{\link[shiny:runGadget]{shiny::runGadget()}} or the background-job
launcher metadata.
}
\description{
Launches the same Claude Code chat core as \code{\link[=claude_addin]{claude_addin()}} in workspace mode.
Sessions are grouped and routed by project, and up to four different threads
may run concurrently. Existing saved projects are included automatically.
}
\examples{
\dontrun{
  claude_workspace_addin()
  claude_workspace_addin(projects = c("~/project-a", "~/project-b"))
}
}
