% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/handlers.R
\name{make_claude_handler}
\alias{make_claude_handler}
\title{Create a ClaudeAgentSDK handler for assistantUIServer}
\usage{
make_claude_handler(
  options = NULL,
  cwd_provider = NULL,
  thinking_provider = NULL,
  models = NULL,
  session_map_path = ".claude_session_map.rds"
)
}
\arguments{
\item{options}{A \code{ClaudeAgentOptions} object. Defaults to
\code{ClaudeAgentOptions(permission_mode = "default", permission_prompt_tool_name = "stdio", include_partial_messages = TRUE)}.}

\item{cwd_provider}{Optional function returning the working directory used
when a thread's CLI client connects. It may declare \code{thread_id} and
\code{project}; arguments are filtered by formals, so existing zero-argument
providers remain compatible.}

\item{thinking_provider}{Optional zero-argument function returning the current
thinking level to apply on connect.}

\item{models}{Optional character vector of model ids to offer in the model
selector (a "Default" option is always prepended).}

\item{session_map_path}{Path to the \code{.rds} file used to persist
\code{thread_id -> session_id} mappings. Defaults to
\code{".claude_session_map.rds"} in the current working directory.}
}
\value{
A \code{coro::async} handler function compatible with \code{\link[=assistantUIServer]{assistantUIServer()}}.
The returned handler declares \code{supports_concurrent_threads = TRUE}: each
thread owns a separate Claude client while normal turns and compaction remain
strict single-consumer operations within that thread. This lets
\code{assistantUIServer(max_concurrent_runs = ...)} run different threads under
its bounded global scheduler.
}
\description{
Wraps \code{ClaudeAgentSDK} into an \code{assistantUIServer}-compatible handler.
Supports streaming, tool approval UI, thinking output, attachments,
and session persistence across R restarts.
}
\examples{
\dontrun{
handler <- make_claude_handler()

server <- function(input, output, session) {
  ctrl <- assistantUIServer("chat", handler = handler,
                            show_thread_list = TRUE)
  # inject sessions into sidebar
  shiny::observe({
    sessions <- list_claude_sessions()
    ctrl$send_sessions(list(sessions = sessions))
  })
}
}

}
