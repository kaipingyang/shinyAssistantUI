% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/handlers.R
\name{ellmer_stream_handler}
\alias{ellmer_stream_handler}
\title{Ellmer Streaming Handler Factory}
\usage{
ellmer_stream_handler(factory)
}
\arguments{
\item{factory}{A zero-argument function that returns a new \code{ellmer} chat
object (e.g. \code{chat_openai_compatible(...)}, \code{chat_openai(...)}, etc.).
Called once per thread the first time a message arrives on that thread.}
}
\value{
A handler function compatible with \code{\link[=assistantUIServer]{assistantUIServer()}}, with
signature \verb{function(message, thread_id, on_chunk, on_done, on_error)}.
}
\description{
Creates an \code{assistantUIServer} handler backed by an \code{ellmer} chat object,
with per-thread conversation history and async streaming support.
}
\details{
Each thread gets its own chat object (created on demand via \code{factory}),
so multi-turn conversation context is preserved within a thread across
multiple user messages.
}
\examples{
\dontrun{
handler <- ellmer_stream_handler(function() {
  ellmer::chat_openai_compatible(
    base_url    = "https://your-endpoint/v1",
    model       = "your-model",
    credentials = function() Sys.getenv("DATABRICKS_TOKEN")
  )
})

assistantUIServer("chat", handler = handler, show_thread_list = TRUE)
}

}
